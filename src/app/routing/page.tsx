//routing-->routing means Which page to show based on the URL in the browser

//static-->Static routes have a fixed, known URL. 
// The page doesn't change based on any parameter.

export default function RoutingHomePage() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-bold text-purple-700"> This is the Static Routing Page</h1>
    </div>
  );
}
